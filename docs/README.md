## Project Docs

Architecture, decisions, and BI concepts.

## Local Runtime Notes
- Streamlit telemetry is disabled via user-level config (~/.streamlit/config.toml)
- The system is designed to run fully local with no external data exfiltration
